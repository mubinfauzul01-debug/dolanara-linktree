Dolanara Animated Linktree — Web Build (Final)

Contents:
- index.html
- style.css
- assets/
    - logo-dolanara.png    (replace with your real logo; keep same filename)
    - favicon.png          (replace with your favicon; same filename)
    - nature-bgm.mp3       (place your chosen music file here, same filename)
    - nature-bgm.ogg       (optional alternative audio format)

Usage:
1. Replace assets/logo-dolanara.png and assets/favicon.png with your original files (same filenames).
2. Put your final music file as assets/nature-bgm.mp3 (and .ogg optional). The site will start the music after the user's first tap/click to comply with browser autoplay policies.
3. Commit all files to the root of your GitHub repo (or upload via Vercel). Vercel will deploy automatically and the site will be available at https://<your-project>.vercel.app
4. If you want a pre-rendered MP4 background instead of the SVG scene, edit index.html and insert a <video> element.

Notes:
- The included assets are placeholders (small transparent PNGs). Replace them with high-quality files before publishing.
